{
    "name": "inventory-ledger-system",
    "version": "1.0.0",
    "description": "Simple, local-language-first inventory & ledger software",
    "scripts": {
        "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "start": "npm run dev"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}